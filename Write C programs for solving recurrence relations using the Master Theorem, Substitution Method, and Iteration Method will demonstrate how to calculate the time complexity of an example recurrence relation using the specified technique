#include <stdio.h>
#include <math.h>

int main() {
    int a = 2;
    int b = 2;
    int f_n = 1; 

    double log_b_a = log(a) / log(b);
    printf("Log_b(a) = %f\n", log_b_a);

    printf("The time complexity T(n) is O(n^%f) = O(n)\n", log_b_a);

    return 0;
}
